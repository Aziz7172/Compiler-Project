# شرح مشروع Flask_Compiler

## نظرة عامة

`Flask_Compiler` هو أداة سطر أوامر مكتوبة بلغة **Java (8+)** هدفها تحويل مشروع ويب مبني بأسلوب **Flask (Python) + قوالب Jinja2** إلى **موقع إلكتروني ثابت (Static HTML)** يعمل بدون خادم، مع الحفاظ على التفاعلية عبر `JavaScript` وتخزين البيانات محلياً في `localStorage`.

تخيّل أن لديك تطبيق Flask بسيط فيه صفحات وعرض بيانات، تريد نشره على استضافة ثابتة لا تدعم Python — هذه الأداة تقرأ كود Flask وتُخرِج صفحات HTML جاهزة.

---

## مسار المعالجة (Pipeline)

يتم المعالجة عبر المراحل التالية (كما هو مُنفَّذ فعلياً في الكود):

```
Python (.py) ──► ANTLR Lexer/Parser ──► AST ──► Semantic Analysis ──► Code Generation ──► HTML
Jinja2 (.jinja) ──► ANTLR Lexer/Parser ──► AST ──► Jinja2 Semantic Check ──► (نفس مرحلة التوليد)
```

1. **التحليل النحوي (Parsing)**: باستخدام ANTLR مع ملفات القواعد
   - `PythonLexer.g4` (140 سطراً) + `PythonParser.g4` (434 سطراً) لتحليل كود Python.
   - `Jinja2Lexer.g4` (90 سطراً) + `Jinja2Parser.g4` (181 سطراً) لتحليل قوالب Jinja2.
   - المولّد (Antlr4) نفسه موجود في `src/ANTLR/antlr-4.13.2-complete.jar`.
   - الملفات المولّدة تخرج في حزمة `gen.ANTLR` داخل `src/gen/ANTLR/`.

2. **بناء شجرة الصياغة المجردة (AST)**: عُقد في حزمة `src/AST/` (60 ملف Java) تغطي Python و HTML و CSS و Jinja2 (عُقد `Jinja2*Node`) و عبارات Behaviors.

3. **التحليل الدلالي (Semantic Analysis)**:
   - `SemanticAnalyzer.java` (764 سطراً): يفحص الشجرة، يبني **Symbol Table**، يتحقق من الأنواع والمتغيرات غير المعرّفة.
   - `Jinja2SemanticChecker.java` (179 سطراً): فاحص منفصل لسيناريوهات Jinja2 يعتمد على **التعبيرات النمطية (Regex)** فوق نص القالب (وليس شجرة ANTLR) — يفحص: الكتل غير المغلقة، المتغيرات غير المعرّفة، والتكرار فوق نوع غير صالح.

4. **توليد الكود (Code Generation)**:
   - `HtmlCodeGenerator.java` (1064 سطراً) هو المولّد الفعلي النشط — يحوّل AST إلى HTML ويعالج دوال `len` و `list` و `isTruthy` و `resolveFromNode`.
   - يوجد `CodeGenerator.java` قديم/معطّل لم يُستخدم في المسار الفعلي.

5. **الآلة الافتراضية (VM)** — **مكتوبة لكن غير موصولة بالمسار**:
   - `src/VM/BytecodeInstruction.java`, `BytecodeGenerator.java` (345), `VMExecutor.java` (507): تصميم لتنفيذ كود بايت، لكنه حالياً كود ميت لا يُستدعى من `CompilerPipeline`.

6. **الإخراج النهائي**: `TemplateManager.java` (481 سطراً) ينسخ ملفات `script.js` و `style.css` من جذر المشروع إلى مجلد `output/` ويولّد صفحات `.html` من قوالب `.jinja`.

---

## نقطة الدخول

- **`src/Main.java`** (106 سطراً) → يتسلم مسارات الملفات والقالب، ويستدعي:
- **`src/CompilerPipeline.java`** (66 سطراً) → يدير المراحل بالترتيب، ويولّد التقارير.
- **`src/TemplateManager.java`** (481 سطراً) → يدير القوالب والملفات الثابتة والإخراج النهائي.

---

## شجرة المشروع الفعلية (بعد التدقيق)

```
Flask_Compiler-master/
│
├── app.py                     ← تطبيق Flask العيّني (36 سطراً): 3 مسارات + بيانات منتجات/طلاب
├── README.md                  ← التعليمات (يذكر antlr-4.13.1-complete.jar — القديم)
├── script.js                  ← تفاعلية الصفحات (366 سطراً): CRUD عبر localStorage
├── style.css                  ← تنسيقات الموقع (271 سطراً)
├── توثيق_المشروع.md           ← توثيق سابق موجود (لم يتم تعديله)
├── شرح المشروع.md             ← هذا الملف
│
├── templates/                 ← قوالب Jinja2 (6 ملفات)
│   ├── index.jinja            ← لوحة التحكم: بطاقات إحصائية + شريط تنقّل
│   ├── products.jinja         ← جدول المنتجات + شريط بحث + أزرار Edit/Delete/View
│   ├── students.jinja         ← 13 سطراً: {% for student in students %}
│   ├── add_product.jinja      ← نموذج إضافة (id="productForm" class="generic-form")
│   ├── edit_product.jinja     ← نموذج تعديل + زر حذف (.btn-danger)
│   └── product_details.jinja  ← صفحة تفاصيل المنتج (detailName/detailPrice/detailDetails)
│
├── src/                       ← مصادر جافا
│   ├── Main.java              (106)
│   ├── CompilerPipeline.java  (66)
│   ├── TemplateManager.java   (481)
│   │
│   ├── ANTLR/                 ← قواعد ANTLR + الـ jar
│   │   ├── PythonLexer.g4     (140)
│   │   ├── PythonParser.g4    (434)
│   │   ├── Jinja2Lexer.g4     (90)
│   │   ├── Jinja2Parser.g4    (181)
│   │   └── antlr-4.13.2-complete.jar
│   │
│   ├── gen/ANTLR/             ← الملفات المولّدة من ANTLR
│   │
│   ├── AST/                   ← 60 عقدة شجرة (Python + HTML + CSS + Jinja2 + Behaviors)
│   │
│   ├── SemanticAnalysis/
│   │   ├── SemanticAnalyzer.java     (764)
│   │   ├── SemanticReporter.java     (95)
│   │   ├── SemanticError.java        (45)
│   │   └── Jinja2SemanticChecker.java (179)
│   │
│   ├── ErrorHandling/
│   │   └── CompilerErrorListener.java (117)  ← تقرير الأخطاء النحوية
│   │
│   ├── CodeGenerator/
│   │   ├── HtmlCodeGenerator.java    (1064)  ← المولّد النشط
│   │   └── CodeGenerator.java                ← معطّل (قديم)
│   │
│   ├── VM/                            ← كود ميت (غير موصول)
│   │   ├── BytecodeInstruction.java
│   │   ├── BytecodeGenerator.java     (345)
│   │   └── VMExecutor.java            (507)
│   │
│   └── Tests/                ← نماذج اختبار Flask
│       ├── add_product.py
│       ├── delete_product.py
│       ├── product_details.py
│       ├── show_products.py
│       └── templates/        ← نسخ HTML للتوقع
│           ├── add_product.html
│           ├── product_details.html
│           └── products.html
│
├── output/                   ← الموقع الثابت الناتج (8 ملفات)
│   ├── index.html
│   ├── products.html
│   ├── product_details.html
│   ├── add_product.html
│   ├── edit_product.html
│   ├── students.html
│   ├── style.css
│   └── script.js
│
├── compiler_output/          ← تقارير المترجم الناتجة (5 ملفات)
│   ├── syntax_report.txt
│   ├── semantic_report.txt
│   ├── ast_python.json
│   ├── ast_jinja.json
│   └── generation_log.txt
│
├── out/                      ← مخرجات الترجمة (.class) من IntelliJ
├── .idea/                    ← إعدادات مشروع IntelliJ IDEA
├── Flask_Compiler.iml        ← ملف تعريف وحدة IntelliJ
└── tmp_argfile.txt           ← ملف وسيط لعناوين ملفات javac (Argument File)
```

---

## مكونات تقنية بالتفصيل

### 1) نموذج التطبيق: `app.py`
تطبيق Flask عيّني بنمط السطور (مماثل لملفات الاختبار):
- المسارات:
  - `/` → `home`
  - `/products` → `show_products`
  - `/product/<name>` → `product_details(name)`
- المتغيرات العامة (Globals) الممرّرة للقوالب:
  - `products`: 3 قواميس (منتجات).
  - `product`: منتج واحد.
  - `students`: 3 طلاب.
  - `iterations = [1, 2, 3]`.
  - `user_name = "Admin"`.
- استدعاء العرض يستخدم `render_template("products.html", …)` — لاحظ أنه `.html` وليس `.jinja`، والمترجم يتعامل مع هذا الاختلاف.

### 2) قوالب Jinja2
- `index.jinja`: لوحة تحكم بها بطاقات إحصائية وتنسيقات `<style>` مضمنة وشريط تنقّل (شعار + روابط).
- `products.jinja`: جدول منتجات، شريط بحث، وأزرار الصف (`btn-view`, `btn-edit`, `btn-delete`) مع حالة "لا منتجات" (empty state).
- `students.jinja`: تكرار بسيط `{% for student in students %}` مع تحميل `script.js` في نهاية الصفحة.
- نماذج `add_product.jinja` و `edit_product.jinja`: نماذج بحقول name/price/image/details وخصائص `data-entity="product"`.
- `product_details.jinja`: بطاقة تفاصيل المنتج مع رابط رجوع.
- جميع قوائم التنقّل تستخدم `/` و `/products` و `/add` و `/edit` وتُعاد كتابتها بواسطة `rewriteLinks` في المولّد.

### 3) الواجهة الأمامية: `script.js` + `style.css`
- `script.js` (366 سطراً) يوفّر **تطبيق CRUD كاملاً بدون خادم**:
  - `ensureInitialized()`: يزرع منتجات افتراضية في `localStorage` عند أول تشغيل.
  - `readProducts()` / `writeProducts()`: القراءة والكتابة من `localStorage`.
  - `renderTable()`: يرسم صفوف الجدول ديناميكياً.
  - `renderDetails()`: يملأ صفحة التفاصيل من `localStorage` حسب `?id=`.
  - `deleteProduct(id)`: حذف مع مطابقة آمنة بين النصوص والأرقام.
  - `bindEvents()`: تفويض الحذف على مستوى المستند، تسجيل التعديل، فلترة البحث أثناء الكتابة، واعتراض النماذج (إضافة/تعديل).
  - **ترتيب التشغيل مهم**: `ensureInitialized()` ← `renderTable()` ← `renderDetails()` ← `bindEvents()`.
- `style.css` (271 سطراً): شريط جانبي داكن، بطاقات منتجات، أزرار ملونة (أحمر للحذف، أزرق/أخضر للعرض/التعديل)، شريط بحث، وأنيميشن `fadeIn`/`fadeOut`، وانتقالات سلسة.

### 4) التحليل الدلالي
- **Symbol Table**: يعرّف رموزاً من نوع `SymbolType.VARIABLE` و `CLASS` وغيرها، مع أنواع مستنتجة عبر `inferType(...)` أو `"unknown"`.
- **Panic Mode (وضع التعافي من الأخطاء)**: كل عبارة داخل `if`/`else`/`elif`/دوال و تعبيرات ملفوفة في `try/catch`؛ عند الخطأ يُسجَّل خطأ من نوع `TYPE_ERROR` بعنوان `PANIC_MODE: …` ويُطبع `[PANIC_MODE] Line N: …` في `stderr` — هدفه ألا ينهار المترجم مهما حدث.
- **دوال تحليل Jinja2** (`analyzeJinjaForNode`, `analyzeJinjaIfNode`, `analyzeJinjaExpression`, `registerJinjaKnownVariable(s)`) تفحص المتغيرات المعروفة (`jinjaKnownVariables`) مقابل الـ Symbol Table وتُبلغ عن `UNDEFINED_CONTEXT_VARIABLE`.

### 5) التقارير (في `compiler_output/`)
| الملف | المصدر | المحتوى |
|---|---|---|
| `syntax_report.txt` | `CompilerErrorListener.java` | ترويسة `=== Syntax Error Report ===` + التاريخ + `Total errors: N` + قائمة مرقّمة، يُكتب بـ `Files.writeString(..., UTF_8)` |
| `semantic_report.txt` | `SemanticReporter.writeErrors` | الأخطاء الدلالية المجمّعة |
| `ast_python.json` | المترجم | شجرة Python بصيغة JSON |
| `ast_jinja.json` | المترجم | شجرة Jinja2 بصيغة JSON |
| `generation_log.txt` | `TemplateManager` | سجلّ عملية التوليد |

### 6) ملاحظة حول الإصدارات والأدوات
- ملف القواعد يستخدم حزمة الـ jar **`antlr-4.13.2-complete.jar`** داخل `src/ANTLR/`.
- `README.md` يذكر `antlr-4.13.1-complete.jar` — رقم قديم لا يطابق الجرة الفعلية.
- أمر البناء يستخدم ملف `tmp_argfile.txt` كـ **Argument File** لـ `javac` (قائمة مسارات ملفات `src/**/*.java` بعناوين مفصولة بفواصل وعكس الشرطة المائلة).

---

## المتطلبات (Requirements)

| المتطلب | التفصيل |
|---|---|
| **Java** | إصدار 8 أو أحدث (تُستخدم API مثل `Files.writeString` التي تتطلب Java 11+ عملياً) |
| **ANTLR** | `antlr-4.13.2-complete.jar` لتحليل القواعد وتوليد الـ Lexer/Parser |
| **لا حاجة لخادم** | المخرجات موقع ثابت يعمل بمجرد فتح ملفات HTML |
| **المتصفح** | يدعم `localStorage` و `URLSearchParams` للتفاعلية |

---

## الصعوبات والحلول

| الصعوبة | الحل المتبع |
|---|---|
| المترجم ينهار عند أي خطأ دلالي | **Panic Mode**: عزل كل عبارة في `try/catch` وتسجيل خطأ `TYPE_ERROR` مع الاستمرار |
| التحقق الدلالي لقوالب Jinja2 عبر شجرة ANTLR مركّب | **فاحص Regex** منفصل (`Jinja2SemanticChecker`) يفحص النص مباشرة: كتل غير مغلقة، متغيرات غير معرّفة، تكرار خاطئ |
| اسم ملف القالب في `render_template("products.html")` لا يطابق `.jinja` | طبقة `TemplateManager` تتعامل مع تطابق الأسماء وتولّد الصفحات النهائية |
| التفاعلية (إضافة/تعديل/حذف) مطلوبة في موقع ثابت | تنفيذ CRUD بالكامل عبر `localStorage` في `script.js` مع نسخه تلقائياً إلى `output/` |
| الفرق بين الجرة المذكورة في README والموجودة فعلياً | الجرة الفعلية `4.13.2` داخل `src/ANTLR/` (تحديث README ما زال معلّقاً) |
| كود Generators/VM مكتوب لكن غير موصول | `HtmlCodeGenerator` هو المسار النشط الوحيد حالياً؛ `CodeGenerator` وحزمة `VM` كود ميت للاستخدام المستقبلي |

---

## المزايا (Features)

- تحويل تطبيق Flask + قوالب Jinja2 إلى **موقع ثابت** يعمل بدون خادم.
- توليد تقارير **نحوية ودلالية** مفصّلة إلى `compiler_output/`.
- تصدير شجرتي AST (Python و Jinja2) بصيغة JSON للمراجعة.
- نسخ تلقائي لملفات `script.js` و `style.css` إلى المخرجات.
- تفاعلية كاملة (إضافة/تعديل/حذف/بحث/عرض تفاصيل) بدون أي Backend عبر `localStorage`.
- نظام **Panic Mode** يجعل المترجم مرناً أمام الأخطاء بدل الانهيار.
- بنية قابلة للتوسّع (AST موحّد، مراحل مفصولة، وقواعد ANTLR جاهزة لتعديل اللغة).

---

*أُنشئ هذا المستند بفحص فعلي لملفات المشروع في تاريخ 4 أغسطس 2026.*
