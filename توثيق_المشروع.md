# توثيق مشروع Flask_Compiler

دليل شامل لهيكل مجلدات مشروع `Flask_Compiler` وشرح كل مكوّن. هذا المشروع هو **مترجم قوالب (Jinja2) بلغة جافا** يحوّل قوالب Jinja2 وصفحات Flask إلى موقع HTML ثابت.

---

## 1. نظرة عامة

المشروع يقرأ قوالب `.jinja` من مجلد `templates`، ويحوّلها عبر خط أنابيب (Pipeline) مكوّن من مراحل التحليل النحوي (ANTLR) ثم بناء شجرة الجمل المجردة (AST) ثم التحليل الدلالي (Semantic Analysis) ثم جدول الرموز (Symbol Table) ثم توليد الكود (Code Generator) ثم تنفيذ الآلة الافتراضية (VM) ليُنتج في النهاية ملفات HTML ثابتة في مجلد `output`.

نقطة الدخول: `Main.java` → `CompilerPipeline.java` → `TemplateManager.java` (اكتشاف القوالب) → معالجة كل قالب عبر المراحل أعلاه.

---

## 2. شجرة المجلدات الكاملة

```
Flask_Compiler-master/
│
├── app.py                  ← تطبيق Flask المصدري (النموذج الذي يُحاكى)
├── README.md               ← الوثيقة الرئيسية
├── build_cmd.bat           ← سكربت البناء الرسمي (يُنتج out\)
├── Flask_Compiler.iml      ← ملف مشروع IntelliJ IDEA
├── style.css               ← ورقة الأنماط الجذرية
├── script.js               ← سكربت جافاسكربت جذري
├── .gitignore              ← يستثني out/ و bin/ و .vscode/ وملفات IDE
│
├── .github/
│   ├── copilot-instructions.md
│   └── instructions/
│       └── mermaid.instructions.md
│
├── .idea/                  ← إعدادات IntelliJ
│   ├── .gitignore
│   ├── misc.xml
│   ├── modules.xml
│   ├── vcs.xml
│   └── workspace.xml
│
├── .vscode/
│   └── settings.json
│
├── .playwright-mcp/        ← بيانات أداة Playwright
│
├── src/                    ← المصدر الرئيسي (جافا)
│   ├── .idea/
│   ├── out/                ← ناتج IDE (شجرة كاملة)
│   ├── gen/                ← يحتوي ANTLR/ (مولّد)
│   ├── Main.java
│   ├── CompilerPipeline.java
│   ├── TemplateManager.java
│   ├── ANTLR/              ← 4 ملفات قواعد .g4
│   ├── AST/                ← عُقد شجرة الجمل + الزائر
│   │   └── CSS/
│   ├── CodeGenerator/
│   ├── ErrorHandling/
│   ├── SemanticAnalysis/
│   ├── SymbolTable/
│   ├── VM/
│   └── Tests/
│       └── templates/
│
├── out/                    ← ناتج البناء الرسمي (build_cmd.bat)
│   ├── *.py (6 ملفات فارغة 0 بايت)
│   ├── *.class (قديمة)
│   └── production/
│       └── Flask_Compiler/ ← نسخة كاملة من ناتج IDE
│
├── templates/              ← قوالب المصدر (.jinja)
│   ├── students.jinja
│   ├── product_details.jinja
│   ├── products.jinja
│   ├── index.jinja
│   ├── edit_product.jinja
│   └── add_product.jinja
│
├── output/                 ← الموقع الثابت المُنتَج رسميًا
│   ├── students.html
│   ├── product_details.html
│   ├── products.html
│   ├── index.html
│   ├── edit_product.html
│   ├── add_product.html
│   ├── script.js
│   └── style.css
│
├── compiler_output/        ← تقارير المترجم
│   ├── generation_log.txt
│   ├── ast_python.json
│   ├── ast_jinja.json
│   ├── syntax_report.txt   ← قديم/راكد
│   └── semantic_report.txt ← قديم/راكد
│
├── tests/                  ← اختبارات أخطاء دلالية
│   ├── error_test.jinja
│   ├── test_missing_flask_var.py
│   ├── test_scope_error.py
│   ├── test_type_error.py
│   ├── test_type_mismatch.py
│   └── test_undefined_var.py
│
├── test_template.html      ← صفحة اختبار
├── test_doctype.html       ← صفحة اختبار
│
├── doc/                    ← فارغ
├── python/                 ← فارغ
└── test_app/               ← فارغ
    ├── compiler_output/
    └── output/
```

---

## 3. شرح الوحدات البرمجية (src)

### 3.1 نقطة الدخول وخط الأنابيب
| الملف | الوظيفة |
|---|---|
| `Main.java` | نقطة الدخول الرئيسية للبرنامج |
| `CompilerPipeline.java` | ينسّق مراحل الترجمة بالترتيب (أنتر → AST → دلالات → رموز → توليد → VM) |
| `TemplateManager.java` | يكتشف ملفات القوالب ويديرها |

### 3.2 ANTLR — قواعد اللغة
- `src\ANTLR\` يحتوي **4 ملفات قواعد `.g4`** (تعريفات القواعد النحوية لكل من Jinja2 و Python وغيرها). لا توجد ملفات جافا هنا — الملفات المولّدة تُوضع في مجلدات منفصلة.

### 3.3 AST — شجرة الجمل المجردة
- تحتوي **أكثر من 50 عقدة** تمثّل عناصر اللغة: `ProgramNode`, `IfNode`, `WhileNode`, `ForNode`, `ClassNode`, `FunctionNode`, `ReturnNode`, `PrintNode`, `BinaryOpNode`, `UnaryNode`, `BooleanNode`, `NumberNode`, `StringNode`, `IdentifierNode`, `ListNode`, `TupleNode`, `IndexAccessNode`, `ListComprehensionNode`, `ConnectorNode`, `FromImportNode`, `ParameterNode`, `AssignmentNode`, `FunctionCallNode`, `AttributeAccessNode`, `GlobalNode` وغيرها.
- `ASTVisitor.java`: واجهة الزائر لاجتياز الشجرة.
- `AST\CSS\`: عُقد خاصة بـ CSS — `CssNode.java`, `CssBlockNode.java`, `CssPropertyNode.java`.

### 3.4 SemanticAnalysis — التحليل الدلالي
| الملف | الوظيفة |
|---|---|
| `SemanticAnalyzer.java` | المحلّل الدلالي الرئيسي |
| `SemanticReporter.java` | إعداد/كتابة تقارير الأخطاء الدلالية |
| `SemanticError.java` | تمثيل خطأ دلالي |
| `Jinja2SemanticChecker.java` | فحص دلالي خاص بقوالب Jinja2 |

### 3.5 SymbolTable — جدول الرموز
- `Symbol.java` (رمز)، `Scope.java` (نطاق)، `SymbolTable.java` (الجدول) — لتتبع المتغيرات والنطاقات أثناء الترجمة.

### 3.6 CodeGenerator — توليد الكود
- `CodeGenerator.java` (المولّد العام) و `HtmlCodeGenerator.java` (مولّد HTML).

### 3.7 VM — الآلة الافتراضية
| الملف | الوظيفة |
|---|---|
| `BytecodeGenerator.java` | توليد بايت كود من الشجرة |
| `BytecodeInstruction.java` | تعريف تعليمات البايت كود |
| `VMExecutor.java` | تنفيذ البايت كود |
| `Context.java` | سياق التنفيذ (المتغيرات) |
| `PythonContextExtractor.java` | استخراج سياق Python من الكود |
| `TemplatesToRender.java` | تحديد القوالب المراد تجهيزها |

### 3.8 ErrorHandling — معالجة الأخطاء
- `CompilerErrorListener.java`: مستمع أخطاء ANTLR المخصص.

### 3.9 Tests — اختبارات
- `src\Tests\` يحتوي ملفات بايثون تجريبية: `show_products.py`, `product_details.py`, `delete_product.py`, `add_product.py` مع مجلد `templates\` (نسخ HTML).

---

## 4. القوالب والموقع المُنتَج

### 4.1 قوالب المصدر — `templates\` (6 قوالب)
| القالب | الوظيفة |
|---|---|
| `index.jinja` | الصفحة الرئيسية |
| `products.jinja` | قائمة المنتجات |
| `product_details.jinja` | تفاصيل المنتج |
| `add_product.jinja` | إضافة منتج |
| `edit_product.jinja` | تعديل منتج |
| `students.jinja` | صفحة الطلاب |

### 4.2 الموقع الثابت المُنتَج — `output\`
- 6 ملفات HTML بنفس أسماء القوالب (الأحدث بتاريخ 08/03/2026) + `script.js` + `style.css`.
- ملاحظة: `style.css` في `output` نسخة منسوخة وليست مولّدة حديثًا (تاريخها 07/29).

---

## 5. نواتج الترجمة والأنتر

| المجلد | المحتوى |
|---|---|
| `out\` | **البناء الرسمي** (مجمّع بواسطة `build_cmd.bat`): ملفات `.class` + 6 ملفات `.py` فارغة (0 بايت: students, product_details, products, edit_product, add_product, index) |
| `out\production\Flask_Compiler\` | نسخة كاملة من ناتج IDE (تحتوي نسخًا من كل الحزم) |

---

## 6. تقارير المترجم — `compiler_output\`
- `generation_log.txt`: سجل آخر تشغيل ناجح — وجد 6 قوالب، السياق: `app=null, products=[3], product, students=[3], iterations=[1,2,3], user_name=Admin, context_size=6, templates=[6]`، وولّد 6 صفحات HTML.
- `ast_python.json` و `ast_jinja.json`: تصدير شجرة الجمل بصيغة JSON.
- `syntax_report.txt` و `semantic_report.txt`: تقارير قديمة (غير محدّثة).

---

## 7. الاختبارات الجذرية — `tests\`
- `error_test.jinja` + 5 اختبارات بايثون للأخطاء الدلالية: متغير مفقود، خطأ نطاق، خطأ نوع، عدم تطابق نوع، متغير غير معرّف.

---

## 8. ملاحظات أخيرة
- المجلدات `doc\`, `python\`, `test_app\compiler_output\`, `test_app\output\` فارغة حاليًا.
- البناء الرسمي يتم عبر: `build_cmd.bat` وينتج في `out\`، والتوليد الرسمي يُكتب في `output\`.
